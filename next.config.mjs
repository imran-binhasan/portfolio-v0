/** @type {import('next').NextConfig} */
const nextConfig = {
    target: 'serverless', // Optimizes Next.js for serverless functions (Netlify)
    // You can add more configurations depending on your project needs
  };
  
  export default nextConfig;
  